# Secrets — never commit
scripts/.env
*.env
.env*

# OS / editor cruft
.DS_Store

# Local Claude Code state (may contain command output or pasted secrets)
.claude/settings.local.json
.claude/bash-logs/
